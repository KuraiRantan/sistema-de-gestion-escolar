// Person model
export enum TypeOfIdentifications {
  IC = "Identity Card",
  CC = "Citizenship Card",
  CR = "Civil Registration",
  FI = "Foreigner Id",
}

export enum Gender {
  M = "Male",
  F = "Female",
  O = "Other",
}

// Attendance model
export enum AttendanceStatus {
  PRESENT = "Present",
  ABSENT = "Absent",
  LATE = "Late",
}
